# Bybit API Credentials
# Get your API keys from: https://www.bybit.com/app/user/api-management
# IMPORTANT: Enable "Contract Trading" permissions for futures

BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here

# Exchange Settings
BYBIT_TESTNET=true
# BYBIT_DEMO=false  # Optional: use demo trading (virtual money, no risk)

# IMPORTANT NOTES:
# 1. Copy this file to .env and fill in your real credentials
# 2. NEVER commit .env to git (it's in .gitignore)
# 3. Start with testnet=true for testing
# 4. Switch to testnet=false only when ready for real trading
# 5. Environment variables override config.json settings
