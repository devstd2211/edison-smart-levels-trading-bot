// Keep this local to the web-client so Vite does not need a runtime value import
// from the CommonJS contracts package. The shared contract uses the same default.
export const DEFAULT_CONTROL_BACKUP_KEEP_COUNT = 10;
