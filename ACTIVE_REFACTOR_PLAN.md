# Active Refactor Plan

This file is the active source of truth for open refactor tasks only.
Completed history is intentionally removed from this file and kept in `REFACTOR_PLAN.md`.

## Open Tasks Migrated (2026-03-09)
- [ ] Move optional services behind feature toggles with explicit "capability" interfaces.
- [ ] Update tests to build only the required groups (no global container).
- [ ] Core any cleanup (phase 3: src).
- [ ] Update tests to use `createServices()` + explicit `start/stop`.

## Current Execution Focus
- [ ] Continue `Update tests to use createServices() + explicit start/stop` in lifecycle-adjacent suites.
- [ ] Continue `Update tests to build only the required groups (no global container)` in bot/lifecycle-focused tests.

## Immediate Next Candidates
- [ ] Extend explicit lifecycle teardown coverage beyond `bot-factory` / `trading-bot` suites into the next lifecycle-adjacent test slices (`create-services.lifecycle`, `trading-bot.create-services`, then remaining initializer-adjacent boundaries).
- [ ] Continue replacing broad service-state construction in tests with the minimal required grouped services or tracked `createServices()` state.
- [ ] Continue the compact harness stream with the next untouched constructor-heavy unit/error-handling suites beyond the now-refreshed `bot-metrics` / `performance-analytics` / `exchange-factory` / `orderbook-imbalance` / `funding-rate-filter` / `compound-interest-calculator` / `entry-confirmation` / `limit-order-executor` / `risk-manager` / `take-profit-manager` / `position-sync` / `volume-profile` / `wall-tracker` / `weight-matrix-calculator` / `orderbook-manager` / `ladder-tp-manager` / `websocket-authentication` / `analyzer-engine` / `position-state-machine` / `trading-journal` / `candle-provider.repository-integration` / `candle-provider.error-handling` / `candle-aggregator` / `public-websocket` / `websocket-manager.error-handling` / `websocket-event-handler.error-handling` / `indicator-precalculation` / `indicator-cache` / `market-condition-analyzer` / `analyzer-registry` / `indicator-registry` / `monitoring-server` / `prometheus-metrics` / `websocket-keep-alive` coverage.

## Working Rules
1. Pick the next unchecked item from this file.
2. Apply minimal behavior-preserving changes only.
3. Run targeted tests for the changed slice.
4. Run `npm run build`.
5. Record only active status here; keep completed batch history in `REFACTOR_PLAN.md`.
6. Keep `NEXT_SESSION_PROMPT.md` short: `Last Completed` + `Next Step`.

## Active Notes
- `types/*` cleanup is complete; do not reopen unless a service refactor requires a compatibility follow-up.
- Non-`services`/non-`types` phase-3 cleanup is complete for the previously targeted boundaries; only reopen when new mixed boundaries are discovered during adjacent work.
- Main remaining stream is compact service-by-service cleanup with dedicated test coverage.
- `handlers/websocket.handler.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining fallback/recovery/logging error-message extraction with shared `getErrorMessage()` and re-running targeted tests plus full `npm run build`.
- `handlers/position.handler.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining monitoring/fallback error-message extraction with shared `getErrorMessage()` and re-running targeted tests plus full `npm run build`.
- `data-collector/database-writer.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining batch/final-write error-message extraction with shared `getErrorMessage()` and re-running targeted tests plus full `npm run build`.
- `console-dashboard.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining dashboard state/update error-message extraction with shared `getErrorMessage()` and re-running targeted tests plus full `npm run build`.
- `multi-strategy/strategy-orchestrator.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining candle-routing/orchestrator-creation error-message extraction with shared `getErrorMessage()` and re-running the multi-strategy suite plus full `npm run build`.
- `event-deduplication.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining degraded-cleanup warning error-message extraction with shared `getErrorMessage()` and re-running targeted tests plus full `npm run build`.
- `ladder-tp-manager.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining partial-close / breakeven / trailing error-message extraction with shared `getErrorMessage()` and re-running targeted tests plus full `npm run build`.
- `limit-order-executor.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining placement / timeout / cancel / fallback error-message extraction with shared `getErrorMessage()` and re-running targeted tests plus full `npm run build`.
- `position-state-machine.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining corrupted-state / corrupted-history recovery message extraction with shared `getErrorMessage()` and re-running targeted tests plus full `npm run build`.
- `funding-rate-filter.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining cache-write error-message extraction with shared `getErrorMessage()` and re-running targeted error-handling tests plus full `npm run build`.
- `multi-strategy/strategy-state-manager.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining strategy-switch failure message extraction with shared `getErrorMessage()` and re-running the multi-strategy suite plus full `npm run build`.
- `ml-signal-validator.service.ts` was cleared from the active candidate list on 2026-03-11 after replacing its remaining inline error-message extraction with shared `getErrorMessage()` and re-running its targeted error-handling suite plus full `npm run build`.
- `lifecycle-manager.service.ts`, `graceful-shutdown.service.ts`, and `event-bus.ts` were cleared from the active candidate list on 2026-03-11 after replacing their remaining inline error-message extraction with shared helpers and re-running targeted tests/build.
- `pattern-recognition.service.ts` was cleared from the active candidate list on 2026-03-12 after replacing its remaining fallback logging error-message extraction with shared `getErrorMessage()` and re-running its targeted error-handling suite plus full `npm run build`.
- `orderbook-manager.service.ts` was cleared from the active candidate list on 2026-03-12 after replacing its remaining WallTracker warning error-message extraction with shared `getErrorMessage()` and re-running its targeted error-handling suite plus full `npm run build`.
- `strategy-loader.service.ts` was cleared from the active candidate list on 2026-03-12 after replacing its remaining load/directory-read error-message extraction with shared `getErrorMessage()` and re-running its targeted error-handling suite plus full `npm run build`.
- `indicator-precalculation.service.ts` was cleared from the active candidate list on 2026-03-12 after replacing its remaining calculation-classification error-message extraction with shared `getErrorMessage()` and re-running its targeted error-handling suite plus full `npm run build`.
- Compact-service cleanup status on 2026-03-12: the remaining obvious inline error-message leftovers in `services/*` are now concentrated in exchange-adapter/partial files (`services/binance/*`, `services/bybit/*`) plus minor builder/utils boundaries; do not expand that stream unless a testability task directly requires it.
- Testability batch status on 2026-03-12: started the `createServices()` + explicit lifecycle stream with tracked teardown helpers and real service-state coverage in `bot-factory` / `trading-bot` suites; continue this stream before reopening adapter cleanup.
- Testability batch status on 2026-03-12 follow-up: extended the tracked lifecycle fixture into `create-services.lifecycle` and `trading-bot.create-services` suites and aligned `bot-initializer.test.ts` setup around a single initializer rebuild helper; continue with the remaining lifecycle-adjacent suites before reopening adapter cleanup.
- Testability batch status on 2026-03-12 follow-up: aligned `bot-initializer.error-handling.test.ts` and `bot-event-emitter.test.ts` around shared setup / teardown helpers so initializer rebuilds and started secondary emitters are cleaned up consistently.
- Testability batch status on 2026-03-12 follow-up: aligned `monitoring-server.test.ts`, `prometheus-metrics.test.ts`, and `websocket-keep-alive.service.test.ts` around local tracked builders and suite-level teardown helpers instead of ad hoc per-test lifecycle cleanup.
- Testability batch status on 2026-03-12 follow-up: added shared `mtf-snapshot-gate` test fixtures and tracked auxiliary gate teardown so the unit / functional / error-handling suites use one startup path and destroy every started gate.
- Testability batch status on 2026-03-12 follow-up: added shared `real-time-risk-monitor` fixtures and explicit `monitor.stop()` teardown; the cache-invalidation suite now exercises the real `position-closed` subscription path instead of isolated `Map` simulations.
- Testability batch status on 2026-03-12 follow-up: added shared `position-monitor` fixtures and service construction helpers; the unit and error-handling suites now share one harness and reuse a local config rebuild helper for time-based-exit scenarios instead of hand-written constructor blocks.
- Testability batch status on 2026-03-12 follow-up: added shared `position-sync` fixtures and service construction helpers so the unit and error-handling suites reuse one dependency harness instead of duplicating local logger/exchange/manager/telegram builders; reviewed `position-sync.service.ts` as a follow-up candidate and left production code unchanged because this slice did not expose a safe behavior-preserving service refactor beyond the new shared test harness.
- Testability batch status on 2026-03-12 follow-up: added shared `position-exiting` fixtures for mocked exchange/telegram/journal/session-stats wiring, reusable config builders, and canonical test positions so the main and functional suites share one harness instead of parallel local builders; reviewed `position-exiting.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: extended the `position-exiting` helper through the functional regression slice so the TP1/breakeven scenario now reuses the same harness and risk-config builders as the main suite while keeping the transactional suite behavior unchanged.
- Testability batch status on 2026-03-12 follow-up: added shared `position-lifecycle` fixtures for repository-backed and in-memory service construction, common config/position/signal builders, and event-bus/logger mocks so the error-handling and P0-safety suites no longer maintain separate local lifecycle harnesses; reviewed `position-lifecycle.service.ts` and left production code unchanged because this slice only needed test-fixture consolidation.
- Testability batch status on 2026-03-12 follow-up: added shared `position-pnl-calculator` fixtures for canonical position creation and ErrorHandler/logger wiring so the unit and error-handling suites no longer duplicate their local position builders; reviewed `position-pnl-calculator.service.ts` and left production code unchanged because this slice only needed shared test-fixture consolidation.
- Testability batch status on 2026-03-12 follow-up: added shared `position-state-machine` logger and file-artifact helpers so the unit suite and error-handling persistence suite reuse the same logger construction and disk-path utilities instead of parallel local setup blocks.
- Testability batch status on 2026-03-12 follow-up: extended the `position-state-machine` helper through the error-handling suite to centralize parent-directory creation and cleanup for state/history files while keeping service behavior unchanged; reviewed `position-state-machine.service.ts` and left production code unchanged because this slice only needed test-fixture consolidation.
- Testability batch status on 2026-03-12 follow-up: extended the shared `position-exiting` helper with optional logger/exchange/telegram/journal/session-stats overrides plus real-scenario TP manager wiring, then aligned the integration, race-condition, and error-handling suites on the same fixture builders instead of local constructor blocks; reviewed `position-exiting.service.ts` and left production code unchanged because this slice only needed test-fixture consolidation.
- Testability batch status on 2026-03-12 follow-up: added shared `dynamic-position-sizer` and `position-scaling` harnesses for canonical logger/config/error-handler setup plus broken-logger fixtures, and extended the repository helper with batched position builders so the sizing/scaling suites and `position-lifecycle.repository-integration` no longer maintain parallel local setup loops; reviewed `dynamic-position-sizer.service.ts`, `position-scaling.service.ts`, and the repository slice and left production code unchanged because this batch only needed test-fixture consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `websocket-manager` harness for canonical config/logger/error-handler/auth/deduplication/keep-alive wiring, then aligned the main and error-handling suites on the same setup and explicit async disconnect teardown instead of parallel constructor blocks; reviewed `websocket-manager.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `websocket-authentication` harness for canonical auth logger/error-handler wiring, then aligned the unit and error-handling suites on the same service construction instead of parallel local logger builders; reviewed `websocket-authentication.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `public-websocket` harness and service factory for canonical config/logger/timeframe/error-handler wiring, then aligned the error-handling suite on the same builder path instead of repeated inline constructor blocks; reviewed `public-websocket.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `websocket-event-handler` harness for canonical mocked lifecycle/exiting/exchange/journal/telegram/logger wiring plus reusable position fixtures, then aligned the error-handling suite on the same setup path instead of parallel local mock constructors; reviewed `handlers/websocket.handler.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `risk-manager` harness for canonical config/logger/error-handler wiring plus reusable signal/position/trade fixtures, then aligned the error-handling suite on the same builders instead of parallel local factory classes/functions; reviewed `risk-manager.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added shared `strategy-manager` builders for canonical loader/merger/error-handler mocks plus default strategy/main-config fixtures, then aligned the error-handling suite on the same setup path instead of repeated local mock constructors; reviewed `strategy-manager.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `telegram` harness for canonical config/logger/error-handler/fetch wiring, then aligned the error-handling suite on the same service construction instead of repeating local mock bootstrapping; reviewed `telegram.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `take-profit-manager` harness for canonical logger/error-handler/config wiring, then aligned the main and error-handling suites on the same builder path instead of repeated local config/logger construction; reviewed `take-profit-manager.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `ladder-tp-manager` harness for canonical config/logger/error-handler/exchange wiring plus reusable position fixtures, then aligned the main and error-handling suites on the same setup path instead of parallel local constructor blocks; reviewed `ladder-tp-manager.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `orderbook-manager` harness for canonical logger/error-handler/wall-tracker wiring plus reusable snapshot/delta/time helpers, then aligned the main and error-handling suites on the same setup path while preserving the legacy no-ErrorHandler stale-data expectations; reviewed `orderbook-manager.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added a shared `wall-tracker` harness for canonical config/logger/error-handler wiring, then aligned the main and error-handling suites on the same builder path instead of repeated local logger/config bootstrapping; reviewed `wall-tracker.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added shared `volume-profile` helpers for canonical logger/config/error-handler wiring plus reusable candle and broken-logger fixtures, then aligned the main and error-handling suites on the same setup path instead of repeated local logger/candle constructors; reviewed `volume-profile.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-12 follow-up: added shared `weight-matrix-calculator` helpers for canonical logger/config/error-handler wiring plus reusable minimal input fixtures, then aligned the main and error-handling suites on the same setup path instead of repeated local config/logger bootstrap; reviewed `weight-matrix-calculator.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-12 for the compact-service cleanup slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/pattern-recognition.error-handling.test.ts packages/core/src/__tests__/services/orderbook-manager.service.error-handling.test.ts packages/core/src/__tests__/services/strategy-loader.error-handling.test.ts` -> PASS (3/3 suites, 77/77 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the indicator pre-calculation cleanup slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/indicator-precalculation.error-handling.test.ts` -> PASS (1/1 suite, 20/20 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the lifecycle testability slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/bot-factory.service.test.ts packages/core/src/__tests__/services/bot-factory.error-handling.test.ts packages/core/src/__tests__/trading-bot.lifecycle.test.ts` -> PASS (3/3 suites, 57/57 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the lifecycle testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/create-services.lifecycle.test.ts packages/core/src/__tests__/trading-bot.create-services.lifecycle.test.ts packages/core/src/__tests__/bot-initializer.test.ts` -> PASS (3/3 suites, 25/25 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the initializer/event-emitter follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/bot-initializer.error-handling.test.ts packages/core/src/__tests__/bot-event-emitter.test.ts` -> PASS (2/2 suites, 47/47 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the monitoring/websocket lifecycle follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/monitoring-server.test.ts packages/core/src/__tests__/services/prometheus-metrics.test.ts packages/core/src/__tests__/services/websocket-keep-alive.service.test.ts` -> PASS (3/3 suites, 65/65 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `mtf-snapshot-gate` lifecycle follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/mtf-snapshot-gate.test.ts packages/core/src/__tests__/services/mtf-snapshot-gate.functional.test.ts packages/core/src/__tests__/services/mtf-snapshot-gate.error-handling.test.ts` -> PASS (3/3 suites, 44/44 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `real-time-risk-monitor` lifecycle follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/real-time-risk-monitor.service.test.ts packages/core/src/__tests__/services/real-time-risk-monitor.error-handling.test.ts packages/core/src/__tests__/services/real-time-risk-monitor.cache-invalidation.test.ts` -> PASS (3/3 suites, 59/59 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `position-monitor` lifecycle follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/position-monitor.service.test.ts packages/core/src/__tests__/services/position-monitor.error-handling.test.ts` -> PASS (2/2 suites, 46/46 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `position-sync` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/position-sync.service.test.ts packages/core/src/__tests__/services/position-sync.service.error-handling.test.ts` -> PASS (2/2 suites, 47/47 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `position-exiting` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/position-exiting.service.test.ts packages/core/src/__tests__/services/position-exiting.functional.test.ts packages/core/src/__tests__/services/position-exiting.transactional.test.ts` -> PASS (3/3 suites, 64/64 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `position-lifecycle` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/position-lifecycle.error-handling.test.ts packages/core/src/__tests__/services/position-lifecycle.p0-safety.test.ts` -> PASS (2/2 suites, 36/36 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `position-pnl-calculator` / `position-state-machine` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/position-pnl-calculator.service.test.ts packages/core/src/__tests__/services/position-pnl-calculator.error-handling.test.ts packages/core/src/__tests__/services/position-state-machine.service.test.ts packages/core/src/__tests__/services/position-state-machine.error-handling.test.ts` -> PASS (4/4 suites, 101/101 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `position-exiting` testability follow-up slice (integration / race / error-handling):
  - `npm test -- --runInBand packages/core/src/__tests__/services/position-exiting.integration.test.ts packages/core/src/__tests__/services/position-exiting.race-condition.test.ts packages/core/src/__tests__/services/position-exiting.error-handling.test.ts` -> PASS (3/3 suites, 43/43 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `dynamic-position-sizer` / `position-scaling` / repository follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/dynamic-position-sizer.test.ts packages/core/src/__tests__/services/position-scaling.test.ts packages/core/src/__tests__/services/position-lifecycle.repository-integration.test.ts` -> PASS (3/3 suites, 97/97 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `websocket-manager` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/websocket-manager.service.test.ts packages/core/src/__tests__/services/websocket-manager.error-handling.test.ts` -> PASS (2/2 suites, 31/31 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `websocket-authentication` / `public-websocket` / `websocket-event-handler` testability follow-up slices:
  - `npm test -- --runInBand packages/core/src/__tests__/services/websocket-authentication.service.test.ts packages/core/src/__tests__/services/websocket-authentication.error-handling.test.ts packages/core/src/__tests__/services/public-websocket.error-handling.test.ts packages/core/src/__tests__/services/websocket-event-handler.error-handling.test.ts` -> PASS (4/4 suites, 88/88 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `risk-manager` / `strategy-manager` / `telegram` testability follow-up slices:
  - `npm test -- --runInBand packages/core/src/__tests__/services/risk-manager.error-handling.test.ts packages/core/src/__tests__/services/strategy-manager.error-handling.test.ts packages/core/src/__tests__/services/telegram.error-handling.test.ts` -> PASS (3/3 suites, 78/78 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `take-profit-manager` / `ladder-tp-manager` / `orderbook-manager` testability follow-up slices:
  - `npm test -- --runInBand packages/core/src/__tests__/services/take-profit-manager.service.test.ts packages/core/src/__tests__/services/take-profit-manager.error-handling.test.ts packages/core/src/__tests__/services/ladder-tp-manager.service.test.ts packages/core/src/__tests__/services/ladder-tp-manager.error-handling.test.ts packages/core/src/__tests__/services/orderbook-manager.service.test.ts packages/core/src/__tests__/services/orderbook-manager.service.error-handling.test.ts` -> PASS (6/6 suites, 124/124 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-12 for the `wall-tracker` / `volume-profile` / `weight-matrix-calculator` testability follow-up slices:
  - `npm test -- --runInBand packages/core/src/__tests__/services/wall-tracker.service.test.ts packages/core/src/__tests__/services/wall-tracker.error-handling.test.ts packages/core/src/__tests__/services/volume-profile.service.test.ts packages/core/src/__tests__/services/volume-profile.error-handling.test.ts packages/core/src/__tests__/services/weight-matrix-calculator.service.test.ts packages/core/src/__tests__/services/weight-matrix-calculator.error-handling.test.ts` -> PASS (6/6 suites, 172/172 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-12 follow-up: added shared `compound-interest-calculator` helpers for canonical config/logger/get-balance wiring, then aligned the main and error-handling suites on the same default setup path instead of parallel inline bootstrap blocks; reviewed `compound-interest-calculator.service.ts` and left production code unchanged because this slice only needed shared test-fixture consolidation.
- Verification on 2026-03-12 for the `compound-interest-calculator` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/compound-interest-calculator.service.test.ts packages/core/src/__tests__/services/compound-interest-calculator.error-handling.test.ts` -> PASS (2/2 suites, 66/66 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-12 follow-up: added shared `entry-confirmation`, `exit-type-detector`, and `volatility-regime` helpers for canonical config/logger/error-handler wiring plus reusable position/order fixtures where the detector slice needed them, then aligned the touched suites on the same setup path instead of parallel inline bootstrap blocks; reviewed `entry-confirmation.service.ts`, `exit-type-detector.service.ts`, and `volatility-regime.service.ts` and left production code unchanged because this batch only needed shared test-fixture consolidation.
- Verification on 2026-03-12 for the `entry-confirmation` / `exit-type-detector` / `volatility-regime` testability follow-up slices:
  - `npm test -- --runInBand packages/core/src/__tests__/services/entry-confirmation.service.test.ts packages/core/src/__tests__/services/entry-confirmation.error-handling.test.ts packages/core/src/__tests__/services/exit-type-detector.service.test.ts packages/core/src/__tests__/services/exit-type-detector.service.error-handling.test.ts packages/core/src/__tests__/services/volatility-regime.service.test.ts packages/core/src/__tests__/services/volatility-regime.error-handling.test.ts` -> PASS (6/6 suites, 132/132 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-12 follow-up: added shared `funding-rate-filter`, `order-execution-detector`, and `micro-wall-detector` helpers for canonical config/logger/error-handler wiring plus reusable funding/execution/orderbook fixtures, then aligned the touched suites on the same setup path instead of repeated inline bootstrap blocks; reviewed `funding-rate-filter.service.ts`, `order-execution-detector.service.ts`, and `micro-wall-detector.service.ts` and left production code unchanged because this batch only needed shared test-fixture consolidation.
- Verification on 2026-03-12 for the `funding-rate-filter` / `order-execution-detector` / `micro-wall-detector` testability follow-up slices:
  - `npm test -- --runInBand packages/core/src/__tests__/services/funding-rate-filter.service.test.ts packages/core/src/__tests__/services/funding-rate-filter.error-handling.test.ts packages/core/src/__tests__/services/order-execution-detector.service.test.ts packages/core/src/__tests__/services/order-execution-detector.error-handling.test.ts packages/core/src/__tests__/services/micro-wall-detector.service.test.ts packages/core/src/__tests__/services/micro-wall-detector.error-handling.test.ts` -> PASS (6/6 suites, 123/123 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-12 follow-up: added shared `orderbook-imbalance`, `trading-journal`, and `whale-detection` helpers for canonical config/logger/error-handler wiring plus reusable journal temp-dir/entry-exit and whale analysis fixtures, then aligned the touched suites on the same setup path instead of repeated inline bootstrap blocks; reviewed `orderbook-imbalance.service.ts`, `trading-journal.service.ts`, and `whale-detection.service.ts` and left production code unchanged because this batch only needed shared test-fixture consolidation.
- Verification on 2026-03-12 for the `orderbook-imbalance` / `trading-journal` / `whale-detection` testability follow-up slices:
  - `npm test -- --runInBand packages/core/src/__tests__/services/orderbook-imbalance.service.test.ts packages/core/src/__tests__/services/orderbook-imbalance.error-handling.test.ts packages/core/src/__tests__/services/trading-journal.service.test.ts packages/core/src/__tests__/services/trading-journal.error-handling.test.ts packages/core/src/__tests__/services/whale-detector.service.test.ts packages/core/src/__tests__/services/whale-detection.error-handling.test.ts` -> PASS (6/6 suites, 126/126 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-12 follow-up: added shared `retest-entry`, `tf-alignment`, and `circuit-breaker` helpers for canonical config/logger/error-handler wiring plus reusable retest signal/candle fixtures and alignment indicator/logger builders, then aligned the touched suites on the same setup path instead of repeated inline bootstrap blocks; reviewed `retest-entry.service.ts`, `tf-alignment.service.ts`, and `circuit-breaker.service.ts` and left production code unchanged because this batch only needed shared test-fixture consolidation.
- Verification on 2026-03-12 for the `retest-entry` / `tf-alignment` / `circuit-breaker` testability follow-up slices:
  - `npm test -- --runInBand packages/core/src/__tests__/services/retest-entry.service.test.ts packages/core/src/__tests__/services/retest-entry.error-handling.test.ts packages/core/src/__tests__/services/tf-alignment.service.test.ts packages/core/src/__tests__/services/tf-alignment.error-handling.test.ts packages/core/src/__tests__/services/circuit-breaker.service.test.ts packages/core/src/__tests__/services/circuit-breaker.error-handling.test.ts` -> PASS (6/6 suites, 129/129 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `resilience` lifecycle helpers for canonical mocked logger/error-handler wiring plus tracked `start()/stop()` cleanup, then aligned the `rate-limiter`, `retry-policy`, `bulkhead`, and `resilience-coordinator` suites on the same harness instead of repeating inline interval-backed service bootstrap and teardown; reviewed `services/resilience/rate-limiter.service.ts`, `services/resilience/retry-policy.service.ts`, `services/resilience/bulkhead.service.ts`, and `services/resilience/resilience-coordinator.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-13 for the `resilience` lifecycle/testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/resilience/rate-limiter.test.ts packages/core/src/__tests__/services/resilience/retry-policy.test.ts packages/core/src/__tests__/services/resilience/bulkhead.test.ts packages/core/src/__tests__/services/resilience/resilience-coordinator.test.ts` -> PASS (4/4 suites, 90/90 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `graceful-shutdown` helpers for canonical mocked lifecycle/action-queue/exchange/logger/event-bus wiring plus reusable shutdown position and filesystem bootstrap, then aligned the unit and error-handling suites on the same manager-construction path instead of repeating large inline mock blocks; reviewed `services/graceful-shutdown.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `graceful-shutdown` lifecycle/testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/graceful-shutdown.service.test.ts packages/core/src/__tests__/services/graceful-shutdown.error-handling.test.ts` -> PASS (2/2 suites, 50/50 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `health-check` helpers for canonical logger spy bootstrap plus reusable exchange/websocket/error-handler defaults, then aligned the suite on a single service builder path instead of mixing repeated inline `LoggerService`/mock construction with ad hoc local service instantiation; reviewed `services/health-check.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `health-check` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/health-check.test.ts` -> PASS (1/1 suite, 24/24 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `websocket-keep-alive` helpers for canonical logger/websocket bootstrap, then aligned the suite on one service and socket builder path instead of repeating inline logger creation plus local mock websocket factories around every lifecycle assertion; reviewed `services/websocket-keep-alive.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `websocket-keep-alive` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/websocket-keep-alive.service.test.ts` -> PASS (1/1 suite, 21/21 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `prometheus-metrics` helpers for canonical logger/error-handler bootstrap plus tracked metrics-service lifecycle cleanup, then aligned the suite on one tracked service builder path instead of repeating inline `LoggerService` setup and local tracked construction logic; reviewed `services/prometheus-metrics.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `prometheus-metrics` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/prometheus-metrics.test.ts` -> PASS (1/1 suite, 34/34 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `monitoring-server` helpers for canonical logger/error-handler plus mocked metrics/health service bootstrap and tracked server teardown, then aligned the suite on one server builder path instead of repeating local mock wiring around every endpoint/lifecycle assertion; reviewed `services/monitoring-server.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `monitoring-server` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/monitoring-server.test.ts` -> PASS (1/1 suite, 10/10 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `action-queue` helpers for canonical logger plus reusable action/signal/handler builders, then aligned the error-handling suite on one harness path instead of repeating local queue bootstrap and inline fixture factories; reviewed `services/action-queue.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `action-queue` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/action-queue.error-handling.test.ts` -> PASS (1/1 suite, 26/26 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `event-deduplication` helpers for canonical logger bootstrap plus a reusable service builder, then aligned the suite on one harness path instead of repeated inline logger/service construction across cache, integration, and performance contexts; reviewed `services/event-deduplication.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `event-deduplication` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/event-deduplication.service.test.ts` -> PASS (1/1 suite, 16/16 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `time-service` helpers for canonical logger/exchange/error-handler bootstrap plus a reusable `TimeService` builder with optional exchange attachment, then aligned the error-handling suite on one harness path instead of repeating inline logger/mock exchange/service construction and backward-compatibility rebuilds; reviewed `services/time.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `time-service` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/time.service.test.ts` -> PASS (1/1 suite, 34/34 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `ml-feature-extractor` helpers for canonical mocked logger/error-handler bootstrap plus reusable deterministic candle builders, then aligned the unit and error-handling suites on one harness path instead of repeating inline logger/service setup and parallel local candle factories; reviewed `services/ml-feature-extractor.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `ml-feature-extractor` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/ml-feature-extractor.service.test.ts packages/core/src/__tests__/services/ml-feature-extractor.error-handling.test.ts` -> PASS (2/2 suites, 52/52 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `tick-delta-analyzer` helpers for canonical mocked logger/error-handler/config bootstrap plus reusable tick fixtures, then aligned the unit and error-handling suites on one harness path instead of repeating inline logger/config/service setup and parallel local tick factories; reviewed `services/tick-delta-analyzer.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `tick-delta-analyzer` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/tick-delta-analyzer.service.test.ts packages/core/src/__tests__/services/tick-delta-analyzer.error-handling.test.ts` -> PASS (2/2 suites, 43/43 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `exchange-factory` helpers for canonical mocked logger/error-handler/config bootstrap plus reusable config coercion helpers, then aligned the unit and error-handling suites on one setup path instead of repeating inline logger/config builders and local `ErrorHandler` test doubles; reviewed `services/exchange-factory.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `exchange-factory` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/exchange-factory.service.test.ts packages/core/src/__tests__/services/exchange-factory.error-handling.test.ts` -> PASS (2/2 suites, 51/51 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `delta-analyzer` helpers for canonical real/mock logger bootstrap plus reusable config/tick/signal builders, then aligned the unit and error-handling suites on one setup path instead of duplicating inline logger/config/fixture construction; reviewed `services/delta-analyzer.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `delta-analyzer` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/delta-analyzer.service.test.ts packages/core/src/__tests__/services/delta-analyzer.error-handling.test.ts` -> PASS (2/2 suites, 50/50 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `pnl-calculator` helpers for canonical trade/partial-close fixtures plus reusable mock logger/error-handler bootstrap, then aligned the unit and error-handling suites on one fixture path instead of repeating inline trade arrays, partial-close objects, and ad hoc logger setup; reviewed `services/pnl-calculator.service.ts` and left production code unchanged because this slice only needed test-fixture consolidation.
- Verification on 2026-03-13 for the `pnl-calculator` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/pnl-calculator.service.test.ts packages/core/src/__tests__/services/pnl-calculator.error-handling.test.ts` -> PASS (2/2 suites, 34/34 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `anti-flip` helpers for canonical real/mock logger bootstrap, reusable anti-flip config builders, and shared bullish/bearish candle fixtures, then aligned the unit and error-handling suites on one setup path instead of repeated inline logger/config/candle construction; reviewed `services/anti-flip.service.ts` and left production code unchanged because this slice only needed test-fixture consolidation.
- Verification on 2026-03-13 for the `anti-flip` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/anti-flip.service.test.ts packages/core/src/__tests__/services/anti-flip.error-handling.test.ts` -> PASS (2/2 suites, 39/39 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `bot-metrics` helpers for canonical mock/test logger bootstrap, reusable trade fixtures, and ErrorHandler stubs, then aligned the unit and error-handling suites on one fixture path instead of repeated inline logger subclasses, mock logger objects, and duplicated trade builders; reviewed `services/bot-metrics.service.ts` and left production code unchanged because this slice only needed test-fixture consolidation.
- Verification on 2026-03-13 for the `bot-metrics` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/bot-metrics.service.test.ts packages/core/src/__tests__/services/bot-metrics.error-handling.test.ts` -> PASS (2/2 suites, 70/70 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `config-validator` helpers for canonical logger/error-handler bootstrap plus reusable valid config fixtures and typed config input coercion, then aligned the unit and error-handling suites on one fixture path instead of repeating large inline config objects and local logger/error-handler builders; reviewed `services/config-validator.service.ts` and left production code unchanged because this slice only needed test-fixture consolidation.
- Verification on 2026-03-13 for the `config-validator` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/config-validator.service.test.ts packages/core/src/__tests__/services/config-validator.error-handling.test.ts` -> PASS (2/2 suites, 36/36 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `performance-analytics` helpers for canonical config/logger/journal bootstrap, reusable performance trade fixtures, and ErrorHandler stubs, then aligned the unit and error-handling suites on one fixture path instead of repeating local config objects, mock journal/logger factories, and duplicated trade builders/coercion helpers; reviewed `services/performance-analytics.service.ts` and left production code unchanged because this slice only needed test-fixture consolidation.
- Verification on 2026-03-13 for the `performance-analytics` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/performance-analytics.service.test.ts packages/core/src/__tests__/services/performance-analytics.error-handling.test.ts` -> PASS (2/2 suites, 68/68 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `performance-analytics` helpers with service/harness factories, added shared `exchange-factory` service construction paths, and introduced a dedicated `ladder-exit-detector` harness for canonical logger/exchange/ErrorHandler/order-position fixtures; aligned the three error-handling suites on those helpers instead of repeating direct constructor/bootstrap code and reviewed `services/performance-analytics.service.ts`, `services/exchange-factory.service.ts`, and `services/ladder-exit-detector.service.ts`, leaving production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-14 for the `performance-analytics` + `exchange-factory` + `ladder-exit-detector` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/performance-analytics.error-handling.test.ts packages/core/src/__tests__/services/exchange-factory.error-handling.test.ts packages/core/src/__tests__/services/ladder-exit-detector.service.error-handling.test.ts` -> PASS (3/3 suites, 102/102 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `trading-lifecycle` helpers for canonical mocked logger/event-bus/action-queue/ErrorHandler bootstrap plus tracked manager cleanup, then aligned the error-handling suite on one harness path instead of repeating large inline dependency factories and ad hoc started-manager lifecycle cleanup; reviewed `services/trading-lifecycle.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `trading-lifecycle` lifecycle/testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/trading-lifecycle.error-handling.test.ts` -> PASS (1/1 suite, 35/35 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `trade-history` helpers for canonical logger/ErrorHandler bootstrap, temp-dir lifecycle, and reusable trade record fixtures, then aligned the error-handling suite on one harness path instead of repeating inline `MockLogger` subclasses, ad hoc temp-directory creation, and local ErrorHandler factories; reviewed `services/trade-history.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `trade-history` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/trade-history.error-handling.test.ts` -> PASS (1/1 suite, 30/30 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `virtual-balance` helpers for canonical mock logger/ErrorHandler bootstrap plus temp-dir lifecycle, then aligned the error-handling and integration suites on one harness path instead of repeating inline mock logger objects, fixed test directories, and local service construction; reviewed `services/virtual-balance.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `virtual-balance` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/virtual-balance.error-handling.test.ts` -> PASS (1/1 suite, 35/35 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `trading-orchestrator` helpers for canonical mock logger bootstrap, reusable analysis/entry context constants, and deterministic analyzer/entry error factories, then aligned the error-handling suite on one harness path instead of repeating inline logger objects, duplicated context strings, and local error construction; reviewed `services/trading-orchestrator.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `trading-orchestrator` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/trading-orchestrator.error-handling.test.ts` -> PASS (1/1 suite, 17/17 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `candle-aggregator` helpers for canonical mock logger/ErrorHandler bootstrap plus reusable candle fixtures, then aligned the error-handling suite on one harness path instead of repeating inline logger setup and local candle-building helpers; reviewed `services/candle-aggregator.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `candle-aggregator` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/candle-aggregator.error-handling.test.ts` -> PASS (1/1 suite, 30/30 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `analyzer-engine` helpers for canonical mock logger/registry/analyzer/strategy/candle bootstrap, then aligned the main and advanced error-handling suites on one harness path instead of repeating local analyzer registry factories, mock logger builders, and duplicated candle/config fixtures; reviewed `services/analyzer-engine.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `analyzer-engine` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/analyzer-engine.error-handling.test.ts packages/core/src/__tests__/services/analyzer-engine.error-handling-advanced.test.ts` -> PASS (2/2 suites, 31/31 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `market-condition-analyzer` helpers for canonical mock logger/ErrorHandler bootstrap plus reusable take-profit and flat-market fixtures, then aligned the error-handling suite on one harness path instead of repeating local logger builders, inline `ErrorHandler` setup, and duplicated TP/result factories; reviewed `services/market-condition-analyzer.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `market-condition-analyzer` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/market-condition-analyzer.error-handling.test.ts` -> PASS (1/1 suite, 25/25 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `analyzer-registry` helpers for canonical mock logger/ErrorHandler bootstrap plus reusable base-config and indicator fixtures, then aligned the error-handling suite on one harness path instead of repeating local logger builders, inline registry reconstruction, and duplicated indicator/config factories; reviewed `services/analyzer-registry.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `analyzer-registry` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/analyzer-registry.error-handling.test.ts` -> PASS (1/1 suite, 25/25 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `indicator-registry` helpers for canonical mock logger/ErrorHandler bootstrap plus typed metadata/type coercion helpers, then aligned the error-handling suite on one harness path instead of repeating local logger builders, inline registry reconstruction, and duplicated metadata factories; reviewed `services/indicator-registry.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `indicator-registry` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/indicator-registry.error-handling.test.ts` -> PASS (1/1 suite, 25/25 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `indicator-cache` helpers for canonical mock logger/ErrorHandler/repository bootstrap plus typed cache-key coercion, then aligned the error-handling suite on one harness path instead of repeating local logger builders, inline repository factories, and ad hoc service reconstruction for broken-logger scenarios; reviewed `services/indicator-cache.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `indicator-cache` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/indicator-cache.error-handling.test.ts` -> PASS (1/1 suite, 25/25 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `indicator-precalculation` helpers for canonical logger/cache/candle-provider/calculator bootstrap, then aligned the error-handling suite on one harness path instead of repeating local cache/candle/calculator factories, full service construction, and backward-compatibility rebuild setup; reviewed `services/indicator-precalculation.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `indicator-precalculation` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/indicator-precalculation.error-handling.test.ts` -> PASS (1/1 suite, 20/20 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `advanced-order-flow` helpers for canonical mock logger/config/tick/orderbook bootstrap plus a reusable service harness, then aligned the error-handling suite on one setup path instead of repeating local logger builders, config factories, and full service reconstruction across validation, logging, integration, and backward-compatibility blocks; reviewed `services/advanced-order-flow.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `advanced-order-flow` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/advanced-order-flow.error-handling.test.ts` -> PASS (1/1 suite, 44/44 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `candle-provider` helpers for canonical mock logger/timeframe-provider/exchange/repository bootstrap plus reusable candle fixtures and provider harness construction, then aligned the error-handling suite on one setup path instead of repeating large inline dependency factories and provider reconstruction across retry/skip/recovery scenarios; reviewed `providers/candle.provider.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `candle-provider` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/candle-provider.error-handling.test.ts` -> PASS (1/1 suite, 20/20 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `event-handlers` helpers for canonical mocked position-manager/exiting/exchange/websocket/journal/telegram/logger bootstrap plus reusable position fixtures, then aligned the combined error-handling suite on one harness path instead of repeating parallel constructor blocks for `PositionEventHandler` and `WebSocketEventHandler`; reviewed `services/handlers/position.handler.ts` and `services/handlers/websocket.handler.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `event-handlers` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/event-handlers.error-handling.test.ts` -> PASS (1/1 suite, 27/27 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: added shared `advanced-order-state-machine` helpers for canonical mock logger/ErrorHandler bootstrap plus a reusable service harness, then aligned the main suite on one setup path instead of repeating local mock logger construction and duplicate backward-compatibility service rebuilds; reviewed `services/advanced-order-state-machine.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `advanced-order-state-machine` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/advanced-order-state-machine.test.ts` -> PASS (1/1 suite, 40/40 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: extended the shared `analyzer-engine` helpers with a reusable service harness, then aligned the main service suite on the same builders already used by the error-handling slices instead of maintaining a second local copy of analyzer/registry/config/candle/logger factories; reviewed `services/analyzer-engine.service.ts` again and left production code unchanged because this slice only needed further test-harness consolidation.
- Verification on 2026-03-13 for the `analyzer-engine` service-suite testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/analyzer-engine.service.test.ts` -> PASS (1/1 suite, 28/28 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-13 follow-up: extended the shared `trading-journal` helpers with reusable open/close-trade builders plus the same temp-dir harness path for both suites, then aligned the main and error-handling suites on those helpers instead of repeating large inline trade payloads, ad hoc temp-directory setup, and local logger bootstrapping; reviewed `services/trading-journal.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-13 for the `trading-journal` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/trading-journal.service.test.ts packages/core/src/__tests__/services/trading-journal.error-handling.test.ts` -> PASS (2/2 suites, 49/49 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `anomaly-detection` helpers for canonical logger/ErrorHandler bootstrap, mock-logger variants, reusable trade fixtures, and history-seeding helpers, then aligned the error-handling suite on that harness instead of repeating local service construction and inline trade/logger setup; reviewed `services/anomaly-detection.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `anomaly-detection` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/anomaly-detection.error-handling.test.ts` -> PASS (1/1 suite, 35/35 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `liquidity-heatmap` helpers for canonical logger/ErrorHandler bootstrap, typed config/orderbook coercion, and reusable normal/thin/deep orderbook fixtures, then aligned the error-handling suite on that harness instead of repeating local logger/config/service/orderbook builders; reviewed `services/liquidity-heatmap.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `liquidity-heatmap` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/liquidity-heatmap.error-handling.test.ts` -> PASS (1/1 suite, 43/43 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `smart-order-placement` helpers for canonical logger/ErrorHandler bootstrap, typed config/orderbook coercion, and reusable normal/thin orderbook fixtures, then aligned the error-handling suite on that harness instead of repeating local logger/config/service/orderbook builders; reviewed `services/smart-order-placement.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `smart-order-placement` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/smart-order-placement.error-handling.test.ts` -> PASS (1/1 suite, 33/33 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `phase-10-integration` helpers for canonical phase-service bootstrap, reusable phase signal/context builders, shared orderbook fixtures, and anomaly baseline seeding, then aligned the broad integration suite on that harness instead of repeating local logger/service/config construction across the file; reviewed the related phase-10 service set and left production code unchanged because this slice only needed integration-harness consolidation.
- Verification on 2026-03-14 for the `phase-10-integration` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/phase-10-integration.test.ts` -> PASS (1/1 suite, 12/12 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `pattern-recognition` helpers for canonical logger/ErrorHandler bootstrap, reusable candle/swing fixtures, typed coercion helpers, and direct internal-access typing, then aligned the error-handling suite on that harness instead of repeating local service construction and inline candle/logger builders; reviewed `services/pattern-recognition.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `pattern-recognition` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/pattern-recognition.error-handling.test.ts` -> PASS (1/1 suite, 40/40 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `whale-detection` helpers through the error-handling suite so config/logger/service construction now flows through the same harness already used by adjacent tests instead of repeating local detector bootstrap and backward-compat setup; reviewed `services/whale-detection.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `whale-detection` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/whale-detection.error-handling.test.ts` -> PASS (1/1 suite, 16/16 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `whale-wall-tp` helpers for canonical logger/ErrorHandler bootstrap, reusable wall fixtures, and nested config overrides, then aligned the error-handling suite on that harness instead of repeating local service/config/logger setup across validation, integration, and backward-compat blocks; reviewed `services/whale-wall-tp.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `whale-wall-tp` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/whale-wall-tp.error-handling.test.ts` -> PASS (1/1 suite, 22/22 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `enhanced-exit` helpers for canonical logger/ErrorHandler bootstrap and reusable default config wiring, then aligned the error-handling suite on that harness instead of repeating local mock logger creation and service construction across validation, integration, update-config, edge-case, and backward-compat blocks; reviewed `services/enhanced-exit.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `enhanced-exit` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/enhanced-exit.error-handling.test.ts` -> PASS (1/1 suite, 25/25 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `structure-aware-exit` helpers for canonical config/logger/ErrorHandler bootstrap plus reusable swing-point, liquidity-zone, and volume-profile fixtures, then aligned both the main and error-handling suites on that harness instead of repeating local nested config construction and service setup; reviewed `services/structure-aware-exit.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `structure-aware-exit` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/structure-aware-exit.error-handling.test.ts packages/core/src/__tests__/services/structure-aware-exit.service.test.ts` -> PASS (2/2 suites, 45/45 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `risk-calculator` helpers for canonical mock logger/ErrorHandler bootstrap plus reusable default input and take-profit builders, then aligned the error-handling suite on that harness instead of repeating inline mock logger state and default calculation inputs; reviewed `services/risk-calculator.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `risk-calculator` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/risk-calculator.error-handling.test.ts` -> PASS (1/1 suite, 37/37 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `reality-check` helpers for canonical logger bootstrap plus reusable signal/event/analyzer fixtures, then aligned the error-handling suite on that harness instead of repeating local logger, signal, event, and no-logger service builders across the file; reviewed `services/reality-check.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `reality-check` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/reality-check.error-handling.test.ts` -> PASS (1/1 suite, 30/30 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `swing-point-detector` helpers for canonical mock logger/ErrorHandler bootstrap plus reusable candle arrays, failing-loggers, swing-point builders, and typed cast helpers, then aligned the error-handling suite on that harness instead of repeating local service construction and fixture factories throughout the file; reviewed `services/swing-point-detector.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `swing-point-detector` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/swing-point-detector.error-handling.test.ts` -> PASS (1/1 suite, 20/20 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `console-dashboard` helpers for canonical logger/ErrorHandler bootstrap plus reusable harness and position fixtures, then aligned the error-handling suite on that helper surface instead of repeating local `ConsoleDashboardService` construction across validation, integration, edge-case, and backward-compatibility blocks; reviewed `services/console-dashboard.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: extended the shared `swing-point-detector` helpers with an explicit service factory so backward-compatibility validation paths now flow through helper-driven bootstrap instead of repeating direct `SwingPointDetectorService` construction; reviewed `services/swing-point-detector.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: added shared `strategy-circuit-breaker` helpers for canonical logger/ErrorHandler bootstrap plus reusable harness wiring, then aligned the error-handling suite on those helpers instead of repeating local `StrategyCircuitBreakerService` construction across logging-failure, integration, and backward-compatibility scenarios; reviewed `services/strategy-circuit-breaker.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `console-dashboard` + `swing-point-detector` + `strategy-circuit-breaker` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/console-dashboard.error-handling.test.ts packages/core/src/__tests__/services/swing-point-detector.error-handling.test.ts packages/core/src/__tests__/services/strategy-circuit-breaker.error-handling.test.ts` -> PASS (3/3 suites, 66/66 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added shared `strategy-config-merger` helpers for canonical logger/ErrorHandler bootstrap plus reusable default main-config and strategy fixtures, then aligned the error-handling suite on helper-driven construction instead of repeating local `StrategyConfigMergerService` bootstrap across skip, integration, and backward-compatibility scenarios; reviewed `services/strategy-config-merger.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: added shared `strategy-loader` helpers for reusable mocked `ErrorHandler` bootstrap plus explicit service factory wiring around temporary strategy directories, then aligned the error-handling suite on that helper surface instead of repeating local `StrategyLoaderService` construction in setup and backward-compatibility scenarios; reviewed `services/strategy-loader.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: extended the shared `ml-feature-extractor` helpers with an explicit service factory that preserves no-handler bootstrap paths, then aligned the remaining logging-failure and backward-compatibility coverage on helper-driven construction instead of repeating direct `MLFeatureExtractorService` instantiation; reviewed `services/ml-feature-extractor.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `strategy-config-merger` + `strategy-loader` + `ml-feature-extractor` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/strategy-config-merger.error-handling.test.ts packages/core/src/__tests__/services/strategy-loader.error-handling.test.ts packages/core/src/__tests__/services/ml-feature-extractor.error-handling.test.ts` -> PASS (3/3 suites, 76/76 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `indicator-cache` helpers with explicit service factory wiring that preserves repository-only and logger-only backward-compatibility paths, then aligned the remaining error-handling suite constructor sites on helper-driven bootstrap instead of repeating direct `IndicatorCacheService` construction; reviewed `services/indicator-cache.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: extended the shared `market-condition-analyzer` helpers with an explicit service factory for canonical logger/ErrorHandler bootstrap and no-handler paths, then aligned the remaining error-handling suite constructor sites on that helper surface instead of repeating direct `MarketConditionAnalyzerService` construction; reviewed `services/market-condition-analyzer.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: added shared `multi-timeframe-trend` helpers for canonical logger/ErrorHandler bootstrap plus reusable candle/data builders and swing-detector wiring, then aligned the error-handling suite on helper-driven construction instead of repeating local `MultiTimeframeTrendService` and `SwingPointDetectorService` bootstrap across validation, integration, logging-failure, and backward-compatibility scenarios; reviewed `services/multi-timeframe-trend.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `indicator-cache` + `market-condition-analyzer` + `multi-timeframe-trend` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/indicator-cache.error-handling.test.ts packages/core/src/__tests__/services/market-condition-analyzer.error-handling.test.ts packages/core/src/__tests__/services/multi-timeframe-trend.error-handling.test.ts` -> PASS (3/3 suites, 70/70 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: aligned the `volatility-regime` error-handling suite on the already-existing shared harness so constructor/bootstrap paths now flow through one helper instead of repeating direct service construction for default, failing-logger, disabled-config, and backward-compat cases; reviewed `services/volatility-regime.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `volatility-regime` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/volatility-regime.error-handling.test.ts` -> PASS (1/1 suite, 20/20 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: added a shared `anti-flip` harness for canonical logger/ErrorHandler bootstrap plus reusable service construction, then aligned the error-handling suite on that harness instead of repeating local `AntiFlipService` construction across skip, integration, performance, and edge-case scenarios; reviewed `services/anti-flip.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `anti-flip` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/anti-flip.error-handling.test.ts` -> PASS (1/1 suite, 20/20 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `analyzer-registry` helpers with a reusable service factory, then aligned the error-handling suite on that factory for failing-logger and backward-compatibility scenarios instead of repeating local `AnalyzerRegistryService` construction; reviewed `services/analyzer-registry.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: extended the shared `indicator-registry` helpers with a reusable service factory, then aligned the error-handling suite on that factory for failing-logger and backward-compatibility scenarios instead of repeating local `IndicatorRegistry` construction; reviewed `services/indicator-registry.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: extended the shared `tick-delta-analyzer` helpers with an explicit service factory that preserves invalid-config validation paths, then aligned the error-handling suite on helper-driven construction instead of repeating local `TickDeltaAnalyzerService` bootstrap across validation, backward-compatibility, and recovery scenarios; reviewed `services/tick-delta-analyzer.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `analyzer-registry` + `indicator-registry` + `tick-delta-analyzer` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/analyzer-registry.error-handling.test.ts packages/core/src/__tests__/services/indicator-registry.error-handling.test.ts packages/core/src/__tests__/services/tick-delta-analyzer.error-handling.test.ts` -> PASS (3/3 suites, 72/72 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: aligned the `bot-metrics` error-handling suite on the existing shared service factory so constructor, backward-compatibility, and report/trade scenarios now reuse one helper-driven bootstrap path instead of repeating local `BotMetricsService` construction; reviewed `services/bot-metrics.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: added shared `config-validator` service/harness factories for canonical logger/ErrorHandler bootstrap, then aligned the error-handling suite on those helpers instead of repeating local `ConfigValidatorService` construction across deprecation, validation, analyzer, strategy, and logger-failure scenarios; reviewed `services/config-validator.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: extended the shared `virtual-balance` helpers with an explicit service factory, then aligned constructor-validation coverage on that helper so invalid-deposit paths now reuse the same bootstrap surface as the rest of the suite instead of repeating direct `VirtualBalanceService` construction; reviewed `services/virtual-balance.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `bot-metrics` + `config-validator` + `virtual-balance` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/bot-metrics.error-handling.test.ts packages/core/src/__tests__/services/config-validator.error-handling.test.ts packages/core/src/__tests__/services/virtual-balance.error-handling.test.ts` -> PASS (3/3 suites, 91/91 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `anomaly-detection` helpers with an explicit service factory, extended the shared `candle-aggregator` helpers with a reusable service builder, and added a dedicated `timeframe-weighting` harness for canonical logger/ErrorHandler/bootstrap plus reusable casts and default multi-timeframe fixtures; aligned the three error-handling suites on those helpers instead of repeating direct constructor/bootstrap code and reviewed `services/anomaly-detection.service.ts`, `services/candle-aggregator.service.ts`, and `services/timeframe-weighting.service.ts`, leaving production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-14 for the `anomaly-detection` + `candle-aggregator` + `timeframe-weighting` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/anomaly-detection.error-handling.test.ts packages/core/src/__tests__/services/candle-aggregator.error-handling.test.ts packages/core/src/__tests__/services/timeframe-weighting.error-handling.test.ts` -> PASS (3/3 suites, 92/92 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `advanced-order-flow` helpers with a reusable service factory, extended the shared `trading-journal` helpers with an explicit journal service builder, and extended the shared `weight-matrix-calculator` helpers with a reusable service factory that preserves explicit invalid-config validation paths; aligned the three error-handling suites on those helpers instead of repeating direct constructor/bootstrap code and reviewed `services/advanced-order-flow.service.ts`, `services/trading-journal.service.ts`, and `services/weight-matrix-calculator.service.ts`, leaving production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-14 for the `advanced-order-flow` + `trading-journal` + `weight-matrix-calculator` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/advanced-order-flow.error-handling.test.ts packages/core/src/__tests__/services/trading-journal.error-handling.test.ts packages/core/src/__tests__/services/weight-matrix-calculator.error-handling.test.ts` -> PASS (3/3 suites, 95/95 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: aligned the `trade-history` error-handling suite on the existing temp-dir/error-handler harness so fallback and initialization cases now reuse the same service/bootstrap path instead of repeating direct `TradeHistoryService` construction; reviewed `services/trade-history.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Testability batch status on 2026-03-14 follow-up: aligned the `websocket-authentication` error-handling suite further on the shared auth harness so failing-logger and partial-logger scenarios now reuse one service/bootstrap path instead of repeating local `WebSocketAuthenticationService` construction; reviewed `services/websocket-authentication.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `trade-history` + `websocket-authentication` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/trade-history.error-handling.test.ts packages/core/src/__tests__/services/websocket-authentication.error-handling.test.ts` -> PASS (2/2 suites, 61/61 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: aligned the `tf-alignment` error-handling suite further on the existing shared harness so config-validation, threshold-edge, disabled-service, and backward-compat scenarios now reuse helper-driven service/bootstrap paths instead of repeating direct `TFAlignmentService` construction; reviewed `services/tf-alignment.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-14 for the `trade-history` + `websocket-authentication` + `tf-alignment` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/trade-history.error-handling.test.ts packages/core/src/__tests__/services/websocket-authentication.error-handling.test.ts packages/core/src/__tests__/services/tf-alignment.error-handling.test.ts` -> PASS (3/3 suites, 94/94 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `candle-provider` helpers with explicit `ErrorHandler` and provider service factories while preserving the no-error-handler backward-compatibility path, extended the shared `take-profit-manager` helpers with a reusable manager service builder, and extended the shared `strategy-manager` helpers with an explicit service builder; aligned the three error-handling suites on those helpers instead of repeating direct constructor/bootstrap code and reviewed `providers/candle.provider.ts`, `services/take-profit-manager.service.ts`, and `services/strategy-manager.service.ts`, leaving production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-14 for the `candle-provider` + `take-profit-manager` + `strategy-manager` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/candle-provider.error-handling.test.ts packages/core/src/__tests__/services/take-profit-manager.error-handling.test.ts packages/core/src/__tests__/services/strategy-manager.error-handling.test.ts` -> PASS (3/3 suites, 62/62 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `retest-entry` helpers with explicit service and `ErrorHandler` factories, extended the shared `whale-detection` helpers with an explicit service builder that preserves explicit invalid-config validation paths, and extended the shared `whale-wall-tp` helpers with reusable service and `ErrorHandler` factories that accept lightweight mock loggers; aligned the three error-handling suites on those helpers instead of repeating direct constructor/bootstrap code and reviewed `services/retest-entry.service.ts`, `services/whale-detection.service.ts`, and `services/whale-wall-tp.service.ts`, leaving production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-14 for the `retest-entry` + `whale-detection` + `whale-wall-tp` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/retest-entry.error-handling.test.ts packages/core/src/__tests__/services/whale-detection.error-handling.test.ts packages/core/src/__tests__/services/whale-wall-tp.error-handling.test.ts` -> PASS (3/3 suites, 64/64 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `liquidity-heatmap` helpers with explicit service and `ErrorHandler` factories that preserve explicit invalid-config validation paths, added `position-state-machine` service/harness factories for canonical logger/ErrorHandler bootstrap, and extended the shared `position-pnl-calculator` helpers with an explicit service builder; aligned the three error-handling suites on those helpers instead of repeating direct constructor/bootstrap code and reviewed `services/liquidity-heatmap.service.ts`, `services/position-state-machine.service.ts`, and `services/position-pnl-calculator.service.ts`, leaving production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-14 for the `liquidity-heatmap` + `position-state-machine` + `position-pnl-calculator` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/liquidity-heatmap.error-handling.test.ts packages/core/src/__tests__/services/position-state-machine.error-handling.test.ts packages/core/src/__tests__/services/position-pnl-calculator.error-handling.test.ts` -> PASS (3/3 suites, 90/90 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `ladder-tp-manager` helpers with explicit service and `ErrorHandler` factories for canonical config/logger/exchange bootstrap, extended the shared `compound-interest-calculator` helpers with an explicit service builder, and extended the shared `circuit-breaker` helpers with explicit service and `ErrorHandler` factories; aligned the three error-handling suites on those helpers instead of repeating direct constructor/bootstrap code and reviewed `services/ladder-tp-manager.service.ts`, `services/compound-interest-calculator.service.ts`, and `services/circuit-breaker.service.ts`, leaving production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-14 for the `ladder-tp-manager` + `compound-interest-calculator` + `circuit-breaker` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/ladder-tp-manager.error-handling.test.ts packages/core/src/__tests__/services/compound-interest-calculator.error-handling.test.ts packages/core/src/__tests__/services/circuit-breaker.error-handling.test.ts` -> PASS (3/3 suites, 73/73 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-14 follow-up: extended the shared `volume-profile` helpers with explicit service and `ErrorHandler` factories that preserve raw partial-config constructor paths, extended the shared `pattern-recognition` helpers with explicit service and `ErrorHandler` factories that preserve explicit invalid-config validation paths, and extended the shared `smart-order-placement` helpers with explicit service and `ErrorHandler` factories that preserve explicit invalid-config validation paths; aligned the three error-handling suites on those helpers instead of repeating direct constructor/bootstrap code and reviewed `services/volume-profile.service.ts`, `services/pattern-recognition.service.ts`, and `services/smart-order-placement.service.ts`, leaving production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-14 for the `volume-profile` + `pattern-recognition` + `smart-order-placement` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/volume-profile.error-handling.test.ts packages/core/src/__tests__/services/pattern-recognition.error-handling.test.ts packages/core/src/__tests__/services/smart-order-placement.error-handling.test.ts` -> PASS (3/3 suites, 117/117 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-15 follow-up: extended the shared `analyzer-engine` helpers with reusable failing-registry, `ErrorHandler`, service-factory, and harness wiring, then aligned the error-handling suite on helper-driven bootstrap instead of repeating direct `AnalyzerEngineService` and failing-registry construction across analyzer-failure, registry-failure, backward-compatibility, and logging-visibility scenarios; reviewed `services/analyzer-engine.service.ts` and left production code unchanged because this slice only needed test-harness consolidation.
- Verification on 2026-03-15 for the `analyzer-engine` testability follow-up slice:
  - `npm test -- --runInBand packages/core/src/__tests__/services/analyzer-engine.error-handling.test.ts` -> PASS (1/1 suite, 16/16 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-15 follow-up: extended the shared `structure-aware-exit` helpers with an explicit service factory that accepts canonical logger/ErrorHandler wiring, extended the shared `enhanced-exit` helpers with an explicit service factory for canonical logger/ErrorHandler and no-handler bootstrap, and extended the shared `micro-wall-detector` helpers with an explicit detector factory that preserves invalid-config coverage plus optional ErrorHandler injection; aligned the three error-handling suites on those helper-driven construction paths instead of repeating direct service construction across validation, logging-failure, integration, and backward-compatibility scenarios; reviewed `services/structure-aware-exit.service.ts`, `services/enhanced-exit.service.ts`, and `services/micro-wall-detector.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-15 for the `structure-aware-exit` + `enhanced-exit` + `micro-wall-detector` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/structure-aware-exit.error-handling.test.ts packages/core/src/__tests__/services/enhanced-exit.error-handling.test.ts packages/core/src/__tests__/services/micro-wall-detector.error-handling.test.ts` -> PASS (3/3 suites, 81/81 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-15 follow-up: extended the shared `delta-analyzer` helpers so the explicit service factory preserves invalid-config validation paths while still supporting canonical logger/ErrorHandler bootstrap, extended the shared `order-execution-detector` helpers with an explicit service factory plus optional injected ErrorHandler, and extended the shared `risk-calculator` helpers with an explicit calculator factory plus optional injected ErrorHandler; aligned the three error-handling suites on those helper-driven construction paths instead of repeating direct service construction across validation, parsing/logging-failure, integration, and backward-compatibility scenarios; reviewed `services/delta-analyzer.service.ts`, `services/order-execution-detector.service.ts`, and `services/risk-calculator.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `tf-alignment` helpers with an explicit service factory that preserves intentionally invalid config/no-handler constructor paths while keeping the existing harness surface, extended the shared `websocket-authentication` helpers with an explicit service factory plus optional no-ErrorHandler bootstrap, and extended the shared `orderbook-manager` helpers with an explicit manager factory for canonical logger/wall-tracker/ErrorHandler wiring; aligned the three error-handling suites on those helper-driven construction paths instead of repeating direct service construction across validation, stale-snapshot, logging-failure, and backward-compatibility scenarios; reviewed `services/tf-alignment.service.ts`, `services/websocket-authentication.service.ts`, and `services/orderbook-manager.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `event-deduplication` helpers with an explicit service factory plus mockable `ErrorHandler` builder, extended the shared `funding-rate-filter` helpers with an explicit filter factory plus optional no-`ErrorHandler` bootstrap, and extended the shared `orderbook-imbalance` helpers with an explicit service factory for canonical config/logger/ErrorHandler wiring; aligned the three error-handling suites on those helper-driven construction paths instead of repeating direct service construction across retry/cache fallback, validation, logging-failure, and backward-compatibility scenarios; reviewed `services/event-deduplication.service.ts`, `services/funding-rate-filter.service.ts`, and `services/orderbook-imbalance.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `entry-confirmation` helpers with an explicit manager factory plus optional no-`ErrorHandler` bootstrap, extended the shared `anti-flip` helpers with an explicit service factory that supports logger overrides and legacy no-handler construction, and extended the shared `telegram` helpers with an explicit service factory plus optional no-`ErrorHandler` bootstrap; aligned the three error-handling suites on those helper-driven construction paths instead of repeating direct service construction across backward-compatibility, disabled-service, and logger-failure scenarios; reviewed `services/entry-confirmation.service.ts`, `services/anti-flip.service.ts`, and `services/telegram.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `position-sync` helpers with override-based harness creation so custom close-recorder and no-handler constructor paths can reuse the canonical service factory, added a dedicated `limit-order-executor` helper with canonical config/logger/bybit/ErrorHandler bootstrap, and added a dedicated `data-collector` helper with canonical config/logger/database/ErrorHandler bootstrap; aligned the three error-handling suites on those helper-driven construction paths instead of repeating direct service construction across backward-compatibility, cascading-failure, and startup/legacy scenarios; reviewed `services/position-sync.service.ts`, `services/limit-order-executor.service.ts`, and `services/data-collector.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-15 for the `delta-analyzer` + `order-execution-detector` + `risk-calculator` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/delta-analyzer.error-handling.test.ts packages/core/src/__tests__/services/order-execution-detector.error-handling.test.ts packages/core/src/__tests__/services/risk-calculator.error-handling.test.ts` -> PASS (3/3 suites, 78/78 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `tf-alignment` + `websocket-authentication` + `orderbook-manager` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/tf-alignment.error-handling.test.ts packages/core/src/__tests__/services/websocket-authentication.error-handling.test.ts packages/core/src/__tests__/services/orderbook-manager.service.error-handling.test.ts` -> PASS (3/3 suites, 83/83 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `event-deduplication` + `funding-rate-filter` + `orderbook-imbalance` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/event-deduplication.error-handling.test.ts packages/core/src/__tests__/services/funding-rate-filter.error-handling.test.ts packages/core/src/__tests__/services/orderbook-imbalance.error-handling.test.ts` -> PASS (3/3 suites, 60/60 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `entry-confirmation` + `anti-flip` + `telegram` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/entry-confirmation.error-handling.test.ts packages/core/src/__tests__/services/anti-flip.error-handling.test.ts packages/core/src/__tests__/services/telegram.error-handling.test.ts` -> PASS (3/3 suites, 66/66 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `position-sync` + `limit-order-executor` + `data-collector` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/position-sync.service.error-handling.test.ts packages/core/src/__tests__/services/limit-order-executor.error-handling.test.ts packages/core/src/__tests__/services/data-collector.error-handling.test.ts` -> PASS (3/3 suites, 58/58 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-15 follow-up: added dedicated `fractal-smc-weighting` and `ml-signal-validator` helpers for canonical config/logger/ErrorHandler bootstrap plus shared fixture builders, and extended the shared `analyzer-engine` helper typing/factory coverage for the advanced error-handling slice; aligned the three suites on helper-driven construction instead of repeating direct service construction across invalid-config, logging-failure, backward-compatibility, registry-failure, and advanced performance/error scenarios. Reviewed `services/fractal-smc-weighting.service.ts`, `services/ml-signal-validator.service.ts`, and `services/analyzer-engine.service.ts`; left production code unchanged because this batch only needed test-harness consolidation, while stabilizing one existing analyzer-engine timing assertion for low-duration runs.
- Verification on 2026-03-15 for the `fractal-smc-weighting` + `ml-signal-validator` + `analyzer-engine.error-handling-advanced` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/fractal-smc-weighting.error-handling.test.ts packages/core/src/__tests__/services/ml-signal-validator.error-handling.test.ts packages/core/src/__tests__/services/analyzer-engine.error-handling-advanced.test.ts` -> PASS (3/3 suites, 85/85 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-15 follow-up: added a dedicated `session-stats` helper for canonical temp-dir/logger/ErrorHandler/bootstrap plus shared config/trade fixtures, added a dedicated `logger` helper for temp-dir lifecycle and canonical logger/ErrorHandler construction, and extended the shared `position-lifecycle` helper with an explicit ErrorHandler-backed harness; aligned the three suites on those helper surfaces instead of repeating direct service construction across corrupted-file recovery, temp-dir lifecycle, backward-compatibility, and explicit ErrorHandler follow-up scenarios. Reviewed `services/session-stats.service.ts`, `services/logger.service.ts`, and `services/position-lifecycle.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-15 for the `session-stats` + `logger.service` + `position-lifecycle` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/session-stats.error-handling.test.ts packages/core/src/__tests__/services/logger.service.error-handling.test.ts packages/core/src/__tests__/services/position-lifecycle.error-handling.test.ts` -> PASS (3/3 suites, 75/75 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Testability batch status on 2026-03-15 follow-up: extended the shared `risk-manager` helpers with an explicit service factory and configurable logger/ErrorHandler injection, extended the shared `websocket-manager` helpers with an explicit authentication service factory, aligned the `event-deduplication` suite on the existing harness surface for canonical logger/ErrorHandler creation, and added a shared `position-sync` ErrorHandler factory so local logger bootstrap is no longer duplicated; aligned the four suites on helper-driven construction instead of repeating direct service and dependency instantiation across balance-validation, auth-payload, logger/ErrorHandler wiring, and backward-compatibility scenarios. Reviewed `services/risk-manager.service.ts`, `services/websocket-manager.service.ts`, `services/event-deduplication.service.ts`, and `services/position-sync.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: added a dedicated `data-collector` harness for canonical config/logger/ErrorHandler bootstrap, extended the shared `limit-order-executor` helpers with an explicit harness for canonical config/logger/bybit/ErrorHandler construction, and aligned the `micro-wall-detector` error-handling suite on the existing harness surface instead of creating local logger/ErrorHandler pairs in `beforeEach`; aligned the three suites on helper-driven construction instead of repeating direct bootstrap across startup, backward-compatibility, retry/fallback, validation, and logging-failure scenarios. Reviewed `services/data-collector.service.ts`, `services/limit-order-executor.service.ts`, and `services/micro-wall-detector.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `position-monitor` helper so the canonical harness now returns the configured `ErrorHandler` and supports injected logger/no-handler bootstrap, reused the existing `virtual-balance` harness as the canonical logger/ErrorHandler bootstrap source for both suite phases, and extended the shared `weight-matrix-calculator` harness with injectable `ErrorHandler` support; aligned the three error-handling suites on those helper-driven construction paths instead of repeating local `new ErrorHandler(...)` setup across monitoring, persistence/recovery, validation, and threshold scenarios. Reviewed `services/position-monitor.service.ts`, `services/virtual-balance.service.ts`, and `services/weight-matrix-calculator.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: added a dedicated `mtf-snapshot-gate` harness for canonical logger/ErrorHandler bootstrap plus tracked gate cleanup, extended the shared `tick-delta-analyzer` helpers with optional no-handler/bootstrap support while returning the canonical mock logger and `ErrorHandler`, and added an explicit `analyzer-engine` `ErrorHandler` factory so the advanced suite no longer constructs handlers inline; aligned the three suites on those helper-driven construction paths instead of repeating local logger/ErrorHandler/tracked-started-gate setup across cleanup, callback, and validation scenarios. Reviewed `services/mtf-snapshot-gate.service.ts`, `services/tick-delta-analyzer.service.ts`, and `services/analyzer-engine.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: added an explicit `advanced-order-flow` `ErrorHandler` factory and tightened its harness so no-handler construction no longer creates an unused local handler, added a shared `trade-history` service factory so the suite can reuse one canonical bootstrap path for both normal and no-handler variants, and aligned the `volume-profile` suite bootstrap on the existing harness instead of rebuilding logger/ErrorHandler pairs locally; aligned the three suites on helper-driven construction instead of repeating local service/bootstrap setup across validation, migration, logging-failure, and backward-compatibility scenarios. Reviewed `services/advanced-order-flow.service.ts`, `services/trade-history.service.ts`, and `services/volume-profile.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `anomaly-detection` service builder with explicit no-handler support so the suite no longer has to special-case direct service construction, added a reusable `strategy-manager` factory helper for canonical loader/merger/error-handler wiring, and added a reusable `take-profit-manager` factory helper for canonical logger/error-handler bootstrap plus per-test config overrides; aligned the three suites on those helper-driven construction paths instead of repeating local factory closures across validation, console/logging-failure, integration, and backward-compatibility scenarios. Reviewed `services/anomaly-detection.service.ts`, `services/strategy-manager.service.ts`, and `services/take-profit-manager.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: added a reusable `config-validator` factory helper for canonical logger/ErrorHandler bootstrap, added a reusable `compound-interest-calculator` factory helper for canonical logger/getBalance/no-handler construction, and added a reusable `console-dashboard` factory helper for canonical ErrorHandler-backed service construction; aligned the three error-handling suites on those helper-driven construction paths instead of repeating local constructor/bootstrap closures across validation, config-update, logging, integration, and backward-compatibility scenarios. Reviewed `services/config-validator.service.ts`, `services/compound-interest-calculator.service.ts`, and `services/console-dashboard.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: added a reusable `bot-metrics` harness for canonical logger/ErrorHandler/service bootstrap so the unit and error-handling suites share one service-construction path, and aligned the `performance-analytics` service suite on the existing shared harness/service factory instead of rebuilding local config/journal/logger construction in `beforeEach`; reviewed `services/bot-metrics.service.ts` and `services/performance-analytics.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: aligned the `exchange-factory` service suite on the existing shared factory/harness helpers so all factory construction paths reuse one canonical logger/config/ErrorHandler bootstrap, aligned the `orderbook-imbalance` service suite on the existing service helper for disabled and level-override scenarios instead of direct constructor calls, and aligned the `funding-rate-filter` service suite on a local helper-driven service factory so normal, disabled, cache, and fail-safe cases reuse one canonical logger/getFundingRate bootstrap; reviewed `services/exchange-factory.service.ts`, `services/orderbook-imbalance.service.ts`, and `services/funding-rate-filter.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: aligned the `compound-interest-calculator` service suite on the existing shared factory helper so constructor-heavy config variants and error scenarios reuse one canonical logger/getBalance bootstrap, aligned the `entry-confirmation` service suite on the existing helper surface for disabled-manager construction instead of a direct constructor call, and aligned the `limit-order-executor` service suite on the shared logger/config/bybit/service helpers so default and override-based execution paths no longer rebuild local bootstrap inline; reviewed `services/compound-interest-calculator.service.ts`, `services/entry-confirmation.service.ts`, and `services/limit-order-executor.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: aligned the `take-profit-manager` service suite on the existing shared factory helper so all manager construction paths reuse one canonical logger/config bootstrap, aligned the `position-sync` service suite on the existing shared service helper for explicit close-recorder overrides instead of direct constructor calls, and partially aligned the `risk-manager` service suite on the shared harness/helpers so baseline setup plus repeated custom-manager scenarios reuse canonical logger/error-handler/config bootstrap and shared signal/position/trade fixtures; reviewed `services/take-profit-manager.service.ts`, `services/position-sync.service.ts`, and `services/risk-manager.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: aligned the `volume-profile` service suite on the existing shared service helper for disabled, lookback, and tick-size override scenarios instead of direct constructor calls, added an explicit `wall-tracker` service helper and aligned the suite on it for disabled/history-limit scenarios instead of direct constructor calls, and aligned the `weight-matrix-calculator` service suite on the existing shared service helper for disabled calculator variants; reviewed `services/volume-profile.service.ts`, `services/wall-tracker.service.ts`, and `services/weight-matrix-calculator.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: aligned the `orderbook-manager` service suite on the existing shared service harness for baseline logger/bootstrap plus fresh-manager scenarios instead of a direct constructor path, aligned the `ladder-tp-manager` service suite on the existing shared service helper for custom and disabled config scenarios while intentionally preserving direct constructor coverage only in explicit invalid-config tests, and aligned the `websocket-authentication` suite on the shared service factory for fresh-instance integration checks; reviewed `services/orderbook-manager.service.ts`, `services/ladder-tp-manager.service.ts`, and `services/websocket-authentication.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: aligned the `analyzer-engine` service suite on the existing shared service factory/helpers for registry-failure and invalid-signal scenarios instead of direct constructor calls, aligned the `position-state-machine` service suite on the existing shared harness/service factory for baseline initialization and the remaining fresh-service path instead of repeated direct construction, and aligned the `trading-journal` service suite on the existing shared service factory for fresh-instance persistence/reload checks instead of direct constructor calls; reviewed `services/analyzer-engine.service.ts`, `services/position-state-machine.service.ts`, and `services/trading-journal.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: aligned the `candle-provider.repository-integration` suite on the existing shared service factory for canonical provider bootstrap while preserving its real repository and local integration doubles, extended the shared `indicator-precalculation` helper with explicit logger/error-handler/service factories so the suite can reuse one canonical bootstrap path for custom-logger and no-handler scenarios, and trimmed the `indicator-cache` error-handling suite to type/import-only service references now that all construction flows already go through the shared helper surface; reviewed `providers/candle.provider.ts`, `services/indicator-precalculation.service.ts`, and `services/indicator-cache.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `market-condition-analyzer` helper with an explicit ErrorHandler factory and aligned the suite’s logger-failure scenarios on the shared harness instead of ad hoc local service bootstrap, extended the shared `analyzer-registry` and `indicator-registry` helpers with explicit no-handler support while keeping the canonical harness ErrorHandler-backed, and aligned the two suites’ backward-compatibility paths on those shared factories instead of implicit default wiring; reviewed `services/market-condition-analyzer.service.ts`, `services/analyzer-registry.service.ts`, and `services/indicator-registry.service.ts` and left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `candle-aggregator` helper with explicit no-handler/ErrorHandler factory support, extended the shared `public-websocket` helper with an explicit ErrorHandler service factory, and started collapsing the repeated logger/timeframe/exchange/repository/error-handler bootstrap in `candle-provider.error-handling` behind a local scenario builder plus the existing shared harness; aligned the three suites on those helper-driven construction paths instead of repeating direct bootstrap across constructor/backward-compatibility and retry scenarios. Reviewed `services/candle-aggregator.service.ts`, `providers/candle.provider.ts`, and `services/public-websocket.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: extended the shared `websocket-manager` helper with explicit ErrorHandler/service factories so canonical config/logger/auth/deduplication/bootstrap can be reused outside the baseline harness, extended the shared `websocket-event-handler` helper with an explicit handler factory so fresh-instance coverage no longer depends on inline constructor wiring, and continued collapsing `candle-provider.error-handling` bootstrap through a local scenario builder for the repeated RETRY and initialize slices instead of repeating the same logger/timeframe/exchange/repository/ErrorHandler setup per test. Reviewed `services/websocket-manager.service.ts`, `services/handlers/websocket.handler.ts`, and `providers/candle.provider.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Testability batch status on 2026-03-15 follow-up: aligned `monitoring-server.test.ts` directly on the shared server harness instead of a local `createServer` wrapper, extended the shared `prometheus-metrics` helper with an explicit `createService` factory so tracked and ad hoc service construction now use one canonical bootstrap path, and extended the shared `websocket-keep-alive` helper with an explicit service factory so interval/logger override scenarios no longer maintain a local wrapper. Reviewed `services/monitoring-server.service.ts`, `services/prometheus-metrics.service.ts`, and `services/websocket-keep-alive.service.ts`; left production code unchanged because this batch only needed test-harness consolidation.
- Verification on 2026-03-15 for the `risk-manager` + `websocket-manager` + `event-deduplication` + `position-sync` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/risk-manager.error-handling.test.ts packages/core/src/__tests__/services/websocket-manager.error-handling.test.ts packages/core/src/__tests__/services/event-deduplication.error-handling.test.ts packages/core/src/__tests__/services/position-sync.service.error-handling.test.ts` -> PASS (4/4 suites, 87/87 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `data-collector` + `limit-order-executor` + `micro-wall-detector` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/data-collector.error-handling.test.ts packages/core/src/__tests__/services/limit-order-executor.error-handling.test.ts packages/core/src/__tests__/services/micro-wall-detector.error-handling.test.ts` -> PASS (3/3 suites, 69/69 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `position-monitor` + `virtual-balance` + `weight-matrix-calculator` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/position-monitor.error-handling.test.ts packages/core/src/__tests__/services/virtual-balance.error-handling.test.ts packages/core/src/__tests__/services/weight-matrix-calculator.error-handling.test.ts` -> PASS (3/3 suites, 79/79 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `mtf-snapshot-gate` + `tick-delta-analyzer` + `analyzer-engine.error-handling-advanced` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/mtf-snapshot-gate.error-handling.test.ts packages/core/src/__tests__/services/tick-delta-analyzer.error-handling.test.ts packages/core/src/__tests__/services/analyzer-engine.error-handling-advanced.test.ts` -> PASS (3/3 suites, 52/52 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `advanced-order-flow` + `trade-history` + `volume-profile` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/advanced-order-flow.error-handling.test.ts packages/core/src/__tests__/services/trade-history.error-handling.test.ts packages/core/src/__tests__/services/volume-profile.error-handling.test.ts` -> PASS (3/3 suites, 118/118 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `anomaly-detection` + `strategy-manager` + `take-profit-manager` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/anomaly-detection.error-handling.test.ts packages/core/src/__tests__/services/strategy-manager.error-handling.test.ts packages/core/src/__tests__/services/take-profit-manager.error-handling.test.ts` -> PASS (3/3 suites, 77/77 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `config-validator` + `compound-interest-calculator` + `console-dashboard` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/config-validator.error-handling.test.ts packages/core/src/__tests__/services/compound-interest-calculator.error-handling.test.ts packages/core/src/__tests__/services/console-dashboard.error-handling.test.ts` -> PASS (3/3 suites, 74/74 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `bot-metrics` + `performance-analytics.service` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/bot-metrics.service.test.ts packages/core/src/__tests__/services/bot-metrics.error-handling.test.ts packages/core/src/__tests__/services/performance-analytics.service.test.ts` -> PASS (3/3 suites, 99/99 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `exchange-factory.service` + `orderbook-imbalance.service` + `funding-rate-filter.service` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/exchange-factory.service.test.ts packages/core/src/__tests__/services/orderbook-imbalance.service.test.ts packages/core/src/__tests__/services/funding-rate-filter.service.test.ts` -> PASS (3/3 suites, 55/55 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `compound-interest-calculator.service` + `entry-confirmation.service` + `limit-order-executor.service` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/compound-interest-calculator.service.test.ts packages/core/src/__tests__/services/entry-confirmation.service.test.ts packages/core/src/__tests__/services/limit-order-executor.service.test.ts` -> PASS (3/3 suites, 84/84 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `risk-manager.service` + `take-profit-manager.service` + `position-sync.service` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/risk-manager.service.test.ts packages/core/src/__tests__/services/take-profit-manager.service.test.ts packages/core/src/__tests__/services/position-sync.service.test.ts` -> PASS (3/3 suites, 90/90 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `volume-profile.service` + `wall-tracker.service` + `weight-matrix-calculator.service` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/volume-profile.service.test.ts packages/core/src/__tests__/services/wall-tracker.service.test.ts packages/core/src/__tests__/services/weight-matrix-calculator.service.test.ts` -> PASS (3/3 suites, 78/78 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `orderbook-manager.service` + `ladder-tp-manager.service` + `websocket-authentication.service` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/orderbook-manager.service.test.ts packages/core/src/__tests__/services/ladder-tp-manager.service.test.ts packages/core/src/__tests__/services/websocket-authentication.service.test.ts` -> PASS (3/3 suites, 55/55 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `analyzer-engine.service` + `position-state-machine.service` + `trading-journal.service` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/analyzer-engine.service.test.ts packages/core/src/__tests__/services/position-state-machine.service.test.ts packages/core/src/__tests__/services/trading-journal.service.test.ts` -> PASS (3/3 suites, 73/73 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `candle-provider.repository-integration` + `indicator-precalculation.error-handling` + `indicator-cache.error-handling` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/candle-provider.repository-integration.test.ts packages/core/src/__tests__/services/indicator-precalculation.error-handling.test.ts packages/core/src/__tests__/services/indicator-cache.error-handling.test.ts` -> PASS (3/3 suites, 69/69 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `market-condition-analyzer.error-handling` + `analyzer-registry.error-handling` + `indicator-registry.error-handling` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/market-condition-analyzer.error-handling.test.ts packages/core/src/__tests__/services/analyzer-registry.error-handling.test.ts packages/core/src/__tests__/services/indicator-registry.error-handling.test.ts` -> PASS (3/3 suites, 75/75 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `candle-aggregator.error-handling` + `candle-provider.error-handling` + `public-websocket.error-handling` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/candle-aggregator.error-handling.test.ts packages/core/src/__tests__/services/candle-provider.error-handling.test.ts packages/core/src/__tests__/services/public-websocket.error-handling.test.ts` -> PASS (3/3 suites, 74/74 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `candle-provider.error-handling` + `websocket-manager.error-handling` + `websocket-event-handler.error-handling` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/candle-provider.error-handling.test.ts packages/core/src/__tests__/services/websocket-manager.error-handling.test.ts packages/core/src/__tests__/services/websocket-event-handler.error-handling.test.ts` -> PASS (3/3 suites, 65/65 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
- Verification on 2026-03-15 for the `monitoring-server.test.ts` + `prometheus-metrics.test.ts` + `websocket-keep-alive.service.test.ts` testability follow-up batch:
  - `npm test -- --runInBand packages/core/src/__tests__/services/monitoring-server.test.ts packages/core/src/__tests__/services/prometheus-metrics.test.ts packages/core/src/__tests__/services/websocket-keep-alive.service.test.ts` -> PASS (3/3 suites, 65/65 tests).
  - `npm run build` -> PASS (`packages/contracts`, `packages/web-server`, `packages/core`, `packages/web-client`).
